const router = require("express").Router();
const { Post } = require("../../models");

// "/api/post" endpoint

// POST request "/"
// to add a new post

// PUT request "/:id"
// to edit an existing post

// DELETE request ":id"
// to del an exisiting post

module.exports = router;
