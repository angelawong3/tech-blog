const router = require("express").Router();
const { User } = require("../../models");

// "/api/user" endpoint

// POST request "/"
// to sign up

// POST request "/login"
// to login

// POST request"/logout"
// to logout

module.exports = router;
